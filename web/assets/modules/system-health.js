(function(global){
'use strict';const VERSION='4.9.0';
function esc(v){return global.SazmanHRAppBridge?.escape(v)??String(v??'')}
function card(label,value,note,kind='ok'){return `<article class="health-card ${kind}"><span>${esc(label)}</span><strong>${esc(value)}</strong><small>${esc(note||'')}</small></article>`}
async function render(bridge){const host=document.getElementById('systemHealthContent');if(!host)return;host.innerHTML='<div class="panel empty-state compact"><div class="loader"></div><p>در حال بررسی سلامت سامانه…</p></div>';try{const h=await bridge.get('/health');const issues=[];if(h.people_count!==1356)issues.push('تعداد پرسنل');if(h.slide_count!==53)issues.push('تعداد صفحات');if(h.duplicate_ids)issues.push('شناسه تکراری');if(h.duplicate_personnel_numbers)issues.push('شماره پرسنلی تکراری');if(!h.audit_chain_ok)issues.push('زنجیره Audit');host.innerHTML=`<div class="health-hero ${issues.length?'warn':'ok'}"><div><span>وضعیت کلی</span><h2>${issues.length?'نیازمند بررسی':'سالم و آماده بهره‌برداری'}</h2><p>${issues.length?esc(issues.join('، ')):'SQLite، Audit و Baseline پرسنلی بدون خطای بحرانی هستند.'}</p></div><button id="refreshSystemHealth" class="secondary" type="button">بررسی مجدد</button></div><div class="health-grid">${card('پرسنل',bridge.fa(h.people_count),'Baseline مورد انتظار: ۱۳۵۶',h.people_count===1356?'ok':'warn')}${card('صفحات چارت',bridge.fa(h.slide_count),'Baseline مورد انتظار: ۵۳',h.slide_count===53?'ok':'warn')}${card('SQLite',h.source_of_truth==='sqlite'?'منبع اصلی':'نامشخص',`Schema ${h.schema_version}`,h.source_of_truth==='sqlite'?'ok':'warn')}${card('Audit Log',h.audit_chain_ok?'زنجیره سالم':'اختلال در زنجیره',`${bridge.fa(h.audit_count)} رویداد`,h.audit_chain_ok?'ok':'warn')}${card('شناسه تکراری',bridge.fa(h.duplicate_ids),'باید صفر باشد',h.duplicate_ids===0?'ok':'warn')}${card('شماره پرسنلی تکراری',bridge.fa(h.duplicate_personnel_numbers),'باید صفر باشد',h.duplicate_personnel_numbers===0?'ok':'warn')}${card('فاقد واحد',bridge.fa(h.missing_unit),'کنترل کیفیت داده',h.missing_unit===0?'ok':'info')}${card('فاقد عنوان پست',bridge.fa(h.missing_position),'کنترل کیفیت داده',h.missing_position===0?'ok':'info')}${card('خطاهای ثبت‌شده',bridge.fa(h.diagnostic_count),'Error/Crash Log محلی',h.diagnostic_count===0?'ok':'info')}${card('آخرین Backup',h.last_backup_at?bridge.date(h.last_backup_at):'هنوز ثبت نشده','Backup خودکار/دستی',h.last_backup_at?'ok':'info')}${card('آخرین Migration',h.last_migration||'—',`Auth Schema ${h.auth_schema_version}`,'ok')}</div>`;document.getElementById('refreshSystemHealth')?.addEventListener('click',()=>render(bridge));}catch(e){host.innerHTML=`<div class="panel empty-state"><div>!</div><h3>Health Check اجرا نشد</h3><p>${esc(e.message||e)}</p></div>`}}
function mount(bridge){document.querySelector('[data-page="systemHealth"]')?.addEventListener('click',()=>setTimeout(()=>render(bridge),0));}
global.SazmanHRSystemHealth={version:VERSION,mount,render};window.addEventListener('load',()=>global.SazmanHRAppBridge&&mount(global.SazmanHRAppBridge));
})(window);
