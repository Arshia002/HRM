HRM v0.4.0-alpha.3 - Clean Runner Candidate

This is a new candidate built from the failed GitHub branch, not a renamed alpha.2 ZIP.

Local guarded push:
  PUSH-TO-GITHUB.cmd

The push script creates an isolated .venv, installs exact source-gate dependencies,
runs the alpha.3 and package gates, then commits and pushes only after PASS.

Expected GitHub artifact:
  HRM-0.4.0-alpha.3-Tested-Setup

Approved chart remains 536 fixed + 32 named = 568 total; page 16 remains 24.
