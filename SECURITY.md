# سیاست امنیت و داده

این پروژه حاوی دادهٔ منابع انسانی است؛ بنابراین Repository فقط برای سورس، تست و مستندات فنی است.

- دادهٔ واقعی کارکنان، Excel، PowerPoint، SQLite، Backup و Export نباید Commit یا به Artifact عمومی CI افزوده شود.
- رمز عبور، فایل `FIRST_LOGIN.txt`، گواهی امضا و کلید خصوصی TLS نباید وارد Git شوند.
- برای Build عمومی فقط `tools/create_demo_seed.py` مجاز است.
- Seed نهایی باید از فضای کنترل‌شده و با گزینهٔ `--seed` به Build تزریق شود.
- Repository باید Private باشد و دسترسی اعضا حداقلی تنظیم شود.

در صورت مشاهدهٔ افشای احتمالی، Push را متوقف کنید، دسترسی را محدود کنید و تاریخچه و کلیدهای مرتبط را طبق فرایند Incident Response تعویض کنید.
