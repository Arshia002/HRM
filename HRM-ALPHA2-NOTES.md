# HRM v0.2.0-alpha.2

این نسخه یک Build-fix متمرکز پس از شکست alpha.1 است. تغییر اصلی، همسان‌سازی نام خروجی PyInstaller Client با قرارداد کل Pipeline (`HRM.exe`) و اضافه‌کردن Fail-fast contract validation است.

همچنین Source pathهای مستندات Installer به نام‌های ASCII-safe تغییر کرده‌اند تا Extract شدن ZIP روی Windows باعث خرابی Compile در Inno Setup نشود.

هیچ دیتای واقعی پرسنلی یا چارت واقعی در این بسته وجود ندارد.
